package com.yuvraj.interfaces;

/*public class TestC3 implements RightC3, LeftC3 {
	
	
	@Override
	public void m3() {
		System.out.println("This is m3 method form RightC3 interface.");
	}
	
	@Override
	public int m3() {
		return 3;
	}	
	
		  we can't write any java class which implements both method simultaneously.
	      if both interfaces contains a method with same signature but different return types.
		
		  Two methods with the same name and same parameters but different return types
		  cause a conflict because Java can’t decide which return type to use.
		 */
	

