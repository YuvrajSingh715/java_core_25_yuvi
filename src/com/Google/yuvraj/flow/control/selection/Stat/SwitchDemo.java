package com.Google.yuvraj.flow.control.selection.Stat;

public class SwitchDemo {

	public static void main(String[] args) {

		byte x = 10 ;

		switch (x) {
		// Inside this area we print the cases
		// And we can't directly print S.O.P without cases
		// We can take case label only inside the datatype range like for byte = -127 to 128 if you take any number out of range. it will give C.E
		}
	}
}
